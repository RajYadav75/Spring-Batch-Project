spring:
    application:
        name: 01_Spring_Batch
    datasource:
        username: root
        password: root
        url: jdbc:mysql://localhost:3306/sbms1
        driver-class-name: com.mysql.cj.jdbc.Driver
        hikari:
            maximum-pool-size: 20
    jpa:
        hibernate:
          ddl-auto: update
        show-sql: true
    batch:
        jdbc:
            initialize-schema: always